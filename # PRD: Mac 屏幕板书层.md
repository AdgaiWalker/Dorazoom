# PRD: Mac 屏幕板书层
（代号 TBD · 发布前更名，不得使用 ZoomIt 商标）

版本 v1.5 · 状态：需求冻结（经对抗性审理修正）

## 1. Target Outcome

产品上线后，一位从 Windows 迁移到 Mac 的创作者（首要用户即作者本人）
可以在**录制视频**（与 Screen Studio 搭配）和**日常开发**两类场景中，
仅凭一套 `Ctrl+数字` 单手热键语法，在屏幕上即时叠加圈画、打字、白板，
随时 Snip 到剪贴板、一键发起带麦克风的屏幕录制——
实现"边讲边画、边打字"的直播板书体验。
全部功能为**独立图层、可自由组合叠加**；
所有使用行为以本地优先的方式沉淀为可分析的数据资产，
驱动产品自身的功能取舍。

## 2. Problem

1. ZoomIt（Windows / Microsoft Sysinternals）在 Mac 无官方版本。
      Mac 现有替代品（Presentify、Epic Pen、CleanShot X 等）均为
      "模式互斥"设计：要么画、要么打字，无法叠加组合。
2. ZoomIt 自身存在"子模式隐藏"缺陷：打字(T)、白板(W)、换色(R/G/B…)
      均无顶层入口，用户找不到、记不住。
3. ZoomIt 录屏不含声音。
4. Screen Studio 是后期工具（录后自动变焦、光标平滑），
      不覆盖"录制当下"的实时标注需求——两者互补而非竞争。

## 3. Build vs Buy（审理修正案 ①）

不直接购买 Presentify 等现成产品的理由，明牌两条：
1. **组合不存在**：Ctrl+数字 图层开关语法、圈画/打字叠加、
      Snip 直达剪贴板、一键带声录屏，不存在于任何单一产品。
2. **我就是想造**：这首先是一个建造者项目，其次才是一个产品。
      承认这一点意味着诚实的定价——用数月夜晚换取
      "完全合手的工具 + 一个拿得出手的开源作品"。

## 4. Users and Scenarios

- **用户**：作者本人（自用 + 顺手开源发布，免费，GitHub 分发）
- **场景 A · 录制教学视频**：Screen Studio 全屏录制中，
    用 overlay 实时圈画、打字、切白板；后期变焦交给 Screen Studio。
- **场景 B · 日常开发**：圈选代码区域讲解；Snip 区域截图直达剪贴板。
- **场景 C · 突发录屏请求**：按一个热键 3 秒内开始录制
    （画面+标注+麦克风），停止即得 mp4。
    低频但高价值，属"灭火器类"功能。

## 5. Scope

### In Scope（六件套 + 地基）

| 功能 | 热键 | 说明 |
|---|---|---|
| 圈画层 | `Ctrl+2` | 默认冻结态（未授权录屏权限时退化为活屏玻璃）；
自由笔；R/G/B/O/Y/P 六色；`Ctrl+滚轮` 笔宽 |
| 打字层 | `Ctrl+3` | 一等公民入口；点击处浮出真实输入框，
回车烙字上屏（IME 原生支持，含中文） |
| 白板层 | `Ctrl+4` | 一等公民入口（ZoomIt 中为隐藏 `W`） |
| 轻量缩放 | `Ctrl+1` | 仅"放大/还原"两档，不做变焦体系 |
| 讲一段录屏 | `Ctrl+5` | 按下即录（无前置 UI）；麦克风；再按停止存 mp4 至固定位置 |
| Snip→剪贴板 | `Ctrl+6` | 区域拖选；合成当前所有标注层；不落盘；成功时屏幕闪烁 |
| 清场 | `Esc` | 关闭所有层、清除所有标注 |

地基能力：
- **图层架构**：每层独立开关、共享一块透明置顶画布，可任意叠加
- **圈画状态机**：v1 仅实现冻结态（审理修正案 ②）；
    活屏·可画 / 活屏·穿透 入后院，回魂条件见 §9 代理指标
- **权限渐进化**（修正案 ⑤）：零权限即可活屏圈画（即装即用）；
    录屏权限授予后自动解锁冻结态、Snip、缩放、录屏；
    麦克风权限仅录屏需要
- **模式可见性**：菜单栏图标实时显示激活层与状态；
    任意层激活时屏幕角落有常驻标记
- **逐层撤销**：`Ctrl+Z` 撤销当前层最后一笔
- **发现性提示条**：层激活时角落浮出一秒迷你键位提示，用熟后自动消失
- **微反馈**：Snip 成功闪烁、录屏停止菜单栏通知、
    录制中菜单栏红点 + 屏幕角落常驻计时条（可点停，体验红线）
- **事件流遥测**（Phase 0 内建、永久常驻）：
    JSONL 追加日志；记录层开关、Snip/录屏、换色、层组合、
    Esc 后 10 秒内重进圈画（活屏需求代理指标）；
    本地明文、存储位置可配置（指向 iCloud Drive 即得多机同步）、
    一键导出、零网络请求
- **形态**：菜单栏常驻、登录自启、无 Dock 图标、纯 Swift 原生

### Out of Scope（砍单，含回魂条件）

| 砍掉 | 理由 | 回魂条件 |
|---|---|---|
| 计时器、LiveZoom、完整缩放体系 | 与场景无关 / 被 Screen Studio 碾压 | 无 |
| Snip 保存到文件 | 用户明确只要剪贴板 | 遥测显示需求 |
| 多显示器 | 用户单屏；坐标系留缝不实现 | 用户换多屏 |
| 光标高亮、激光笔、聚光灯 | Screen Studio 后期职责 | 无 |
| 黑板（ZoomIt 的 `K`） | 白板用满两周后仍想要再说 | 白板遥测达标后提出 |
| 标注隐藏-恢复 | Esc 清场已覆盖基本需求 | 遥测显示频繁"清场→重画相似内容" |
| 活屏·可画 / 活屏·穿透 | 无使用数据支撑的投机功能（修正案 ②） | 代理指标达标（见 §9） |
| 圈画形状（直线/箭头之外） | 矩形/椭圆推迟 | 遥测说话 |
| 画布内直接编辑文字 | v1 浮出输入框已覆盖（修正案 ④） | 浮出框被证明显著别扭 |
| 系统音频录制 | v1 只录麦克风 | "讲一段"高频后 |
| 视频编辑器/修剪/历史记录 | 与轻量红线冲突 | 永不（精修归 Screen Studio） |
| 账号系统/自建云端 | 信任毒药；订阅制单程票 | 永不（同步走 iCloud 目录） |
| 热键自定义 UI、设置 UI、自动更新 | 自用阶段 JSON 配置即可 | 发布前补做 |

## 6. Requirements

| Requirement | User value | Verification |
|---|---|---|
| overlay 可被外部录制捕获（`sharingType=.readWrite`） | 产品生死线 | Screen Studio 录 10s，成片可见 overlay 内容 |
| Carbon `RegisterEventHotKey` 全局热键 | 免辅助功能权限 | 未授权时 `Ctrl+2` 仍可唤起 |
| 零权限活屏圈画 | 即装即用，首启零弹窗 | 全新安装不授权任何权限即可画 |
| 授权后解锁冻结态 | 标注永不错位 | 授权录屏权限后冻结态自动可用 |
| 图层叠加 | 边画边打字同屏 | 圈画+打字同开，各自独立关闭 |
| Snip 合成标注层入剪贴板 | 素材直达文档/IM | 粘贴结果包含全部可见标注 |
| 录屏（画面+麦克风→mp4） | 3 秒响应突发录屏请求 | 成片有声、标注可见、落固定位置 |
| 浮出输入框打字 | 中文 IME 零成本 | 拼音输入、候选窗正常、回车上屏 |
| 遥测事件流 | 证伪判决与数据资产 | 日志覆盖全部热键事件，schema 文档化 |

## 7. Acceptance Criteria（按阶段）

- [ ] **实验 0（生死线）**：overlay 骨架 + `Ctrl+2` 唤起 + 遥测首行落盘，
            Screen Studio 日常方式录制 10 秒，成片包含 overlay 全部内容
- [ ] **Phase 0（一周硬时限，修正案 ⑥）**：冻结态圈画 + 六色 +
            逐层撤销 + Esc 清场 + 模式可见性 + 遥测上线。
            超时则砍直线/箭头/提示条保交付——
            能天天用的丑工具 > 完美的半成品
- [ ] **Phase 1**：Snip 拖选→剪贴板（合成标注）+成功闪烁；权限引导顺畅
- [ ] **Phase 2**：`Ctrl+5` 一键录/停，mp4 含麦克风声音与标注层，
            录制指示常显；配置文件中输出目录可改
- [ ] **Phase 3**：打字层（浮出输入框），与圈画叠加同屏
- [ ] **Phase 4**：白板层 + 轻量缩放；六件套齐

## 8. 证伪与遥测（审理修正案 ③：标准分层）

- **层类功能**（打字/白板）：频率标准——遥测满 14 天，
    合计激活 <10 次 → 砍回"圈画+Snip"两件套
- **应急类功能**（录屏/Snip）：救场标准——只记录"触发时是否解决问题"，
    永不受频率审判（灭火器不因一年用零次而被扔）
- **图层架构溢价**：层组合出现 0 次 → 重估叠加设计
- **活屏代理指标**（修正案 ②）："Esc 清场后 10 秒内重进圈画"
    频次达标 → 开发活屏态
- **执行纪律**：遥测报告自动生成，但不自动执行；
    报告出具后一周内必须做出砍/留决定并写入 CHANGELOG，
    拖而不决视为留

## 9. Risks and Open Questions

| 风险 | 缓解 |
|---|---|
| overlay 不被录制软件捕获 | 实验 0 最先验证，不通不开发 |
| 动机衰减（自用工具头号死因） | Phase 0 一周硬时限；每周有可用的东西 |
| macOS 15+ 录屏权限周期性弹窗 | ScreenCaptureKit 正路；README 打预防针 |
| 虚拟机/远程桌面吞 `Ctrl+数字` | 撞上再做备用键位 |
| "忘了还在录" | 菜单栏红点+屏幕计时条，体验红线 |
| 命名撞微软商标 | 发布前更名 |

Open questions（均不阻塞开工）：
1. 发布命名（TBD）
2. 录屏/Snip 默认输出位置（桌面 vs 固定文件夹）
3. 活屏态层内切换键位（开发时试手感定）

## 10. Rollout

实验 0 → P0 圈画（一周）→ P1 Snip → P2 录屏 → P3 打字 → P4 白板+缩放
→ 发布清单：更名 / 设置 UI / 热键自定义 UI / 公证（$99/年）/ Sparkle

## 11. 技术栈附录

Swift + AppKit 为主（SwiftUI 仅留设置窗口）· macOS 14+ ·
Carbon RegisterEventHotKey（免辅助功能权限）·
ScreenCaptureKit（SCScreenshotManager / SCStream）·
AVCaptureDevice + AVAssetWriter（mp4）· 矢量笔画模型 + Core Graphics ·
浮出 NSTextField 输入框（v1 打字方案）· JSONL 遥测 ·
SMAppService 登录自启 · 第三方依赖至多一个：KeyboardShortcuts

## 12. 变更记录

- v1.4 → v1.5（对抗性审理六项修正案）：
    ① 补 Build-vs-Buy（含"我想造"明牌）
    ② 冻结态先行，活屏降级后院、回魂改代理指标
    ③ 证伪标准分层：层类看频率、应急类看救场、限期人工执行
    ④ 打字层 v1 改浮出输入框，NSTextInputClient 远期化
    ⑤ 权限渐进化：未授权退化为活屏玻璃
    ⑥ Phase 0 一周硬时限，可砍形状保交付
