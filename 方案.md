# InkLayer 红蓝对抗审理报告 & 修复方案

> 日期：2026-07-23
> 范围：六件套全量源码 + PRD 验收标准
> 原则：只审不改，先把漏洞钉死再动刀

---

## 一、第一性原理：系统必须守住的 6 条不变量

| # | 不变量 | 破了用户会说 |
|---|---|---|
| I1 | **热键一次 = 一次语义**（开或关，不连发） | 「按了没反应 / 秒开秒关」 |
| I2 | **Esc 永远能清掉全部标注层**（录屏除外） | 「Esc 没用，还要再按一遍关」 |
| I3 | **焦点在谁手里，快捷键仍可达** | 「打字时 Ctrl+Z 没用」 |
| I4 | **可见标注 = 可被 Snip/录屏捕获** | 「粘贴没有笔画 / 成片没有字」 |
| I5 | **坐标系一致**（屏幕 / 窗口 / 图像 / Retina） | 「字竖着叠 / 缩放歪 / 截图糊」 |
| I6 | **异步启动可取消、不可半死** | 「录了没文件 / 缩放卡住」 |

---

## 二、红蓝对抗：攻击面穷举

### 2.1 热键与 Esc（I1 / I2）

| ID | 红队假设 | 代码证据 | 判定 |
|---|---|---|---|
| H1 | Carbon 全局 Esc 与层内 `keyCode==53` 双通道，IME 候选窗打开时 Carbon 可能被系统吞 | `HotkeyManager.registerEscape` + 各层 `keyDown` | **P1 残留风险** |
| H2 | 防抖 350ms 挡连发，不同热键互不防抖；快速 Ctrl+2→Esc 正常 | `lastFire[hkID.id]` 按 id 分桶 | 合理 |
| H3 | 全局 Esc 无「是否有激活层」门闩；无层时也 fire，只是不记遥测 | `clearAll` 的 `any` 判断 | 无害 |
| H4 | Snip 容器 keyDown 注释写「仅取消 Snip」，实际 `onCancel` → `clearAll` | `SnipLayer` 23–29 行 vs 142 行注释 | **P2 文档漂移** |
| H5 | 白板永不拿焦点；Esc 完全依赖全局热键 | `WhiteboardLayer` `ignoresMouseEvents=true` | 设计正确 |
| H6 | `RegisterEventHotKey` 失败只打遥测，UI 无提示 | `hotkey.register_failed` | **P1**：冲突时用户以为「Esc 坏了」 |
| H7 | 退出应用不 UnregisterEventHotKey | `HotkeyManager` 无 deinit | **P3**：进程死了系统会清；热重载多实例抢键 |

**Esc 失效排列组合**：
```
Esc 无效 =
  (A) 全局热键未注册成功
  ∨ (B) 只有白板开着 + 全局 Esc 失败（无 key 窗口兜底）
  ∨ (C) IME 标记文本吃掉 Esc
  ∨ (D) 用户以为「关层」=「关菜单栏状态」，实际 isActive 已 false 但菜单未刷
  ∨ (E) 录屏中按 Esc：按设计不关录屏 → 用户误判「Esc 没用」
```

---

### 2.2 焦点与 Ctrl+Z（I3）

| ID | 红队 | 证据 | 判定 |
|---|---|---|---|
| F1 | 打字层焦点在 NSTextField 时画布收不到 Ctrl+Z | 已加 monitor | **已修路径**，待人检 |
| F2 | `Selector(("undo:"))` 字符串选择子脆弱 | TypeLayer 164 行 | **P2** |
| F3 | 圈画层无 local monitor；Snip 后 regainFocus 失败则 Ctrl+Z 失效 | DrawLayer 无 monitor | **P1** |
| F4 | **多交互层同开时只有一个 key 窗口，快捷键「假死」** | 两个层都 `makeKey`，后开的赢 | **P0 架构洞** |
| F5 | 换色键 R/G/B：打字框有内容时 monitor 不换色（防误触） | TypeLayer 214–221 | 合理 |
| F6 | 中文 IME 下 Ctrl+Z 在标记态被故意放过 | monitor hasMarkedText 放行 | 正确 |

**F4 是叠加体验根因级问题**：同开圈画+打字时，只有「当前 key 窗口」那一层能收 R/G/B、Ctrl+Z、滚轮；另一层「看起来开着，快捷键死了」。

---

### 2.3 层叠、Z-order、输入抢占（I4 前半）

| ID | 红队 | 证据 | 判定 |
|---|---|---|---|
| L1 | 白板 level = screenSaver−1，圈画/打字 = screenSaver，白板在下 | WhiteboardLayer / OverlayWindow | 正确 |
| L2 | Zoom / 圈画 / 打字 / Snip 同 level，后 orderFront 盖住先开的 | 无统一 z 序表 | **P1** |
| L3 | Zoom 是整屏不透明黑底+图，不是透镜；开 Zoom = 视觉上「关掉」其它层 | ZoomView.draw 先填黑 | **P1 产品语义** |
| L4 | 录屏 `excludingWindows: []` 应含自家 overlay；sharingType=.readWrite 生死线未自检 | RecordLayer filter | **P0 实验 0 未自动化** |
| L5 | Snip 现路径：FreezeCapture.grab() 全屏，strokes 恒传 [] | LayerCoordinator.startSnip | **P1 回归**：SC 不拍 overlay 则 Snip 丢标注 |
| L6 | 计时条 level = screenSaver+2，会被录进成片 | RecordTickerWindow | **P2 产品选择** |

---

### 2.4 坐标系 / Retina / 中文排版（I5）

| ID | 红队 | 证据 | 判定 |
|---|---|---|---|
| C1 | CGImage 直接 draw(in: bounds)：未处理 CG 顶左 vs AppKit 底左 | CanvasView / ZoomView | **P0 嫌疑**：部分机型冻结图上下颠倒 |
| C2 | display.width/height 是像素，NSScreen.frame 是 pt；Zoom 用 pt 焦点映射像素 | ZoomView.recomputeOffsetKeepingFocus | **P1** |
| C3 | Snip bitmapImageRepForCachingDisplay 未设 scale=backingScaleFactor | compositePNG | **P1**：剪贴板图可能是 1×，Retina 屏糊 |
| C4 | 打字框宽 ≥800 防竖排；烙字用 titleRect 原点 | TypeTypography | **已针对性修** |
| C5 | 烙字 str.size() 与 draw(with: usesLineFragmentOrigin) 混用，宽高与 cell 不完全同构 | TypeCanvasView.draw | **P1**：长中文串回车后相对输入位置轻微漂移 |
| C6 | 输入框固定宽 800+，贴右边缘时文字画出屏幕 | fieldWidth | **P2** |
| C7 | 多显示器一律 NSScreen.main / displays.first | 全库 | **P2 已知砍单** |

---

### 2.5 录屏管线（I6 + 可播性）

| ID | 红队 | 证据 | 判定 |
|---|---|---|---|
| R1 | 帧写入用 wall-clock 限流 30fps，PTS 仍是 SC 原始时间 → 时间戳不连续 | appendVideo lastWriteTime | **P1**：播放器卡顿/时长不准 |
| R2 | 音频 AAC 声明 44.1kHz，麦克风实际可能 48kHz，无格式转换 | AVAssetWriterInput audio settings | **P0**：部分机型整段失败或无声/不可播 |
| R3 | sessionStarted 前丢弃所有音频；视频首帧晚 → 片头无声 | appendAudio | **P2** |
| R4 | boot() 里 guard let display 失败时直接 return，isBooting 可能卡 true | boot 63 行 return 无清 isBooting | **P0 半死锁**：之后 Ctrl+5 全忽略 |
| R5 | stop() 后 isRecording=false，但 seal 异步；快速再开可能双 writer | stop/start 无互斥 | **P1** |
| R6 | 失败删文件：用户只看到「没感觉」 | teardown(abort:) | 已有红闪+beep；错误原因未展示 |
| R7 | 像素缓冲尺寸与 adaptor 声明不一致时 append 静默失败 | 无校验 pb 宽高 | **P1** → frames=0 → 删文件 |
| R8 | 录屏中 Esc 不清录屏（对）但菜单优先显示录制，其它层状态被盖住 | statusPresentation | **P2 可见性** |

**R4 必修路径**：boot() 中 `guard let display = content.displays.first else { return }` ← isBooting 永不复位

---

### 2.6 缩放（Ctrl+1）

| ID | 红队 | 证据 | 判定 |
|---|---|---|---|
| Z1 | 进入时中心固定；滚轮改锚到视图中心，不是进入时焦点 | adjustScale | 与「不跟鼠标」一致；但 + / 滚轮后中心漂移相对初始点 |
| Z2 | 触控板小增量 step*0.5 与鼠标整步混用，手感跳 | scrollWheel | **P2** |
| Z3 | 加载中忽略二次 Ctrl+1；无超时，grab 挂死则永远 loading | isLoading | **P1** |
| Z4 | 缩放层无「在放大图上圈画」——PRD 轻量缩放 OK，但用户可能期望 ZoomIt 式边放边画 | 架构 | **P2 预期差** |

---

### 2.7 Snip / 圈画 / 权限

| ID | 红队 | 证据 | 判定 |
|---|---|---|---|
| S1 | 无权限 Snip：弹窗；有权限但 grab nil：同 denied | startSnip | OK |
| S2 | 圈画无权限 → live-glass 半透明可画，但笔画相对桌面会漂 | DrawLayer mode live-glass | **P1 已知退化**；PRD 接受 |
| S3 | 权限文案「授权后重启生效」——macOS 常需重启 app，未自动检测权限变化 | promptScreenPermission | **P2** |
| S4 | 单击 Snip（无拖）→ selection 0 → tiny-selection 取消，无提示 | mouseUp | **P2** |
| S5 | 圈画 activate 异步期间再按 Ctrl+2：token 作废旧任务，但无 isLoading，连按可能闪 | 对比 Zoom 有 isLoading | **P1** 与历史「秒开秒关」同类 |

---

### 2.8 状态机与菜单栏

| ID | 红队 | 证据 | 判定 |
|---|---|---|---|
| M1 | 层状态只在内存；崩溃后无残留窗口（进程死窗口死） | — | OK |
| M2 | 菜单「墨·圈字」不反映哪一层持有键盘焦点 | statusPresentation | **P1 UX**：看起来都开着，快捷键只服务一层 |
| M3 | 无登录自启 / 无自动更新 | PRD 砍单 | 符合砍单 |
| M4 | Telemetry.queue.sync 在主线程 log 会卡 UI | Telemetry.log | **P3** 高频笔画时微卡 |

---

## 三、压力测试剧本（最可能再炸的操作序列）

| # | 剧本 | 预期炸点 |
|---|---|---|
| 1 | 白板 → 圈画 → 打字 → Esc | 全局 Esc 失败则白板残留 |
| 2 | 打字输入中文候选未上屏 → Esc | 只关候选 / 或清层 |
| 3 | 圈画 + 打字同开 → 在非 key 层按 Ctrl+Z / R | 无响应（**F4**） |
| 4 | 圈画画两笔 → Ctrl+6 Snip | SC 不拍 overlay 则粘贴无笔画（**L5**） |
| 5 | Ctrl+5 → 立刻再 Ctrl+5 | 防抖+isBooting 应挡住；boot 失败则 **R4 卡死** |
| 6 | 录 3 秒停 → QuickTime 开 | 音频采样率不匹配则不可播（**R2**） |
| 7 | Ctrl+1 → 滚轮缩到 1.25 → 再缩 | 到下限应停；触控板可能抖 |
| 8 | Zoom 开着 → Ctrl+2 | 圈画在 Zoom 下/上不定，画在看不见的层 |
| 9 | 打字贴屏幕右缘长句 | 框出屏 |
| 10 | 外接屏当主屏 | 窗口只盖 main |
| 11 | 录屏中开圈画再 Esc | 笔画清掉、录屏继续——正确但易误解 |
| 12 | 无录屏权限 Ctrl+1/5/6 | 应引导；圈画仍可 live-glass |

---

## 四、分级清单

### P0（先修，否则验收不可信）

| ID | 问题 | 建议修法（方向） |
|---|---|---|
| R4 | boot() 无 display 时 isBooting 卡死 | defer/finally 清 isBooting；失败 toast |
| F4 | 多交互层同开时只有一个 key 窗口，快捷键「假死」 | 编排器统一当前输入层；或全局 monitor 分发 Ctrl+Z/换色到顶层交互层 |
| L5 | Snip 只靠全屏截图，丢标注风险 | 恢复合成路径：截屏 + 离屏叠 draw/type 矢量，或 SC 排除校验 |
| C1 | CGImage 绘制是否翻转未统一处理 | 统一 draw 辅助：检测/强制方向 + backingScale |
| R2 | 麦克风采样率与 AAC 设置硬编码不一致 | AVAudioConverter 或按设备实际 rate 写 settings |

### P1（高频体验）

| ID | 问题 |
|---|---|
| H1/H6 | Esc/热键注册失败无感知；IME 与 Esc 竞态 |
| F3 | Snip 后焦点只还 draw 不还 type |
| L2/L3 | Zoom 与其它层 z 序/语义（透镜 vs 冻图） |
| C2/C3/C5 | Retina、Snip 清晰度、烙字基线 |
| R1/R5/R7 | 录屏 PTS、重入、帧尺寸校验 |
| S5 | 圈画激活无 isLoading，连按闪断 |
| Z3 | Zoom grab 无超时 |
| M2 | 菜单不显示「谁持有焦点」 |

### P2 / P3

- 注释漂移、成片含计时条、贴边输入、多屏、Telemetry 同步写盘、热键 unregister、权限变更热更新等。

---

## 五、蓝队：已经相对扎实的部分

- 常量/调色板分离（InkConstants / InkPalette）
- 层业务与 AppDelegate 接线分离（LayerCoordinator）
- 热键防抖 + Zoom/Record 加载中忽略二次按
- 白板穿透 + 低 level
- 打字单行强制 + Ctrl+Z monitor（方向正确，待人检）
- 录屏指示（红条/计时/beep）比早期强
- 遥测覆盖面够做下次验尸

---

## 六、修复战役顺序

### Phase 1：P0 五连修（10 分钟级）

1. **R4**：✅ 录屏 boot 无 display 抛错；teardown 清 isBooting；seal 互斥 isSealing
2. **F4**：✅ InputOwner + 编排器全局 monitor 路由 Ctrl+Z/换色；菜单显示焦点归属
3. **L5 + C1/C3**：✅ Snip 排除 overlay 抓底 + 矢量叠笔画/烙字；ImageDraw 统一方向；bitmap 按 backingScale
4. **R2**：✅ 麦克风采样率跟设备走；帧 PTS 节流 + 尺寸校验；失败通知

### Phase 2：P1 包（高频体验）

- Esc/热键注册失败提示
- Snip 焦点归还 type
- 圈画 isLoading
- Zoom 超时
- Zoom z 序修正
- 菜单显示焦点归属

### Phase 3：P2 打磨

- 输入法与 Esc 竞态
- 贴边输入保护
- 多屏支持
- Telemetry 异步写盘

---

## 七、一句话判决

> **产品形态（六件套开关）已经齐；真正危险的是「多窗口 + 单焦点 + 异步启动 + 截图像素管线」四条缝。**  
> 用户体感上的「Esc 没用 / 要再按一遍解锁 / 快捷键死了」，主因不是单个 Esc bug，而是 **F4 焦点模型** 与 **异步 toggle 竞态** 的组合。  
> 录屏「没感觉 / 播不了」主因是 **R2/R4/R7 管线完整性**，不是 UI 文案。

---

*本文档由红蓝对抗生成，未经修改，保留原文风格。*