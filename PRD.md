# DoraZoom 个人版 PRD

> 状态：产品基线与 Apple 交互重构已落地，用户实测稳定化待完成
> 目标平台：macOS 14 及以上  
> 上游基线：Microsoft Sysinternals ZoomIt for Mac  
> 功能对照：Windows ZoomIt 12.11

## 0. 决策顺序与事实源

遇到功能、交互或实现差异时，按以下顺序裁决，后一项不得覆盖前一项：

1. 本 PRD 中已经确认的 DoraZoom 产品决策。
2. Windows ZoomIt 12.11 的用户可见功能与操作语义。
3. 已锁定提交的官方 ZoomIt for Mac 源码实现。
4. 为满足 macOS 权限、粘贴、媒体兼容和 Apple 交互习惯所做的必要适配。
5. 以上均未规定时，沿用官方 ZoomIt for Mac 默认行为。

`PRD.md` 是产品行为的唯一事实源；具体模块、状态所有权和平台 API 以 `ARCHITECTURE.md` 为唯一事实源。两份文档冲突时，先按上述顺序确认产品行为，再更新架构，不允许实现自行创造第三种行为。

## 1. 目标

基于 Microsoft 官方 ZoomIt for Mac 源码，制作一款供“哆啦”个人使用的 macOS 菜单栏工具。

产品应优先完成“现场讲清楚、截图发出去、教程录下来”三条闭环。DoraZoom 保留 ZoomIt 的核心操作语义与快捷键肌肉记忆，但不以功能数量对齐为目标；低频兼容能力进入高级入口，不与核心动作争夺默认界面和用户注意力。

用户需要能够快速完成以下任务：

- 在屏幕上自由圈画，让对方立即看懂重点。
- 截图后直接粘贴给同事、聊天软件或 AI 修图工具，不留下不必要的本地文件。
- 使用箭头、矩形、直线、高亮和文字辅助讲解。
- 录制包含画面、系统声音、麦克风和摄像头的教程视频。
- 在 macOS 原生播放器和常见剪辑软件中直接使用录制结果。

## 2. 产品原则

1. **核心体验兼容**：保留 ZoomIt 的即时唤起、缩放、圈画、截图、录制和快捷键记忆；不把“官方存在”视为进入默认界面的充分条件。
2. **目的优先**：每项能力必须使核心闭环更快、更清楚、更安全、更可恢复，或解决 Windows 用户迁移到 Mac 的真实阻力。
3. **快捷键遵循 ZoomIt，行为遵循 Mac**：保留肌肉记忆；设置、权限、窗口、菜单、复制粘贴和无障碍遵循 macOS 习惯。
4. **即时响应**：快捷键按下后，光标或等价反馈先于耗时捕获工作出现；动画不得阻塞输入。
5. **用户掌控**：常见操作可立即取消，误操作优先通过撤销和恢复解决，不滥用确认弹窗。
6. **安全负责**：权限按实际功能即时申请；隐私遮挡、录制预检和失败恢复优先于装饰性编辑能力。
7. **简单而非极简**：默认界面只暴露常用路径，高级能力下沉一级，但不通过隐藏必要上下文制造猜测成本。
8. **用完即走、本地优先**：菜单栏常驻、空闲时低占用；默认截图不落盘，不依赖账号、云服务或网络。
9. **官方实现与上游可合并**：优先复用官方捕获、绘制和媒体实现，定制集中在产品适配边界，避免形成无法继续吸收上游更新的重写版本。

## 3. 用户

主要用户为“哆啦”：

- 开发者。
- 教学和教程内容创作者。
- 熟悉 Windows ZoomIt，希望保留原有快捷键记忆。
- 日常使用 Mac，希望复制、粘贴、保存和权限行为符合 macOS 习惯。

## 4. 核心场景

### 4.1 圈画讲解

用户按快捷键进入缩放或原比例绘画，在任意屏幕内容上使用画笔、直线、箭头、矩形、椭圆、高亮和文字进行说明。

### 4.2 截图并发送

用户框选屏幕区域，图片直接进入系统剪贴板；随后可使用 `Command+V` 或兼容模式下的 `Control+V` 粘贴到目标应用，不产生本地截图文件。

### 4.3 截图交给 AI

用户截取界面或内容后，直接粘贴到 AI 工具中进行分析、修改或生成，不需要经过“保存文件—选择文件—上传”的流程。

### 4.4 教程录制

用户录制全屏、区域或窗口，可包含系统声音、麦克风、摄像头画中画和圈画内容；结束后可以预览、裁剪、拼接并导出。

### 4.5 白板讲解

用户在绘画状态中按 `W` 或 `K`，立即进入白板或黑板继续书写、绘图、撤销、截图或录制。

### 4.6 原生文字标注

用户在绘画状态按 `T` 或 `Shift+T` 后，直接使用当前 macOS 输入源输入文字。中文拼音、日文、韩文等组合输入必须显示系统候选窗口和未提交文本；英文大小写、输入源切换、选择、删除、复制、粘贴、撤销等行为与普通 Mac 文本编辑一致。完成后的文字成为普通标注，继续进入截图和录制结果。

## 5. 功能范围

### 5.1 必须交付的产品能力

默认界面的核心能力：

- 静态缩放、实时缩放、原比例绘画和实时绘画。
- 自由画笔、直线、矩形、椭圆、箭头、半透明高亮和文字。
- 彩色画笔、白色/黑色画笔、白板/黑板、撤销和可恢复的清除全部。
- 当前视口复制、区域截图到剪贴板、区域 OCR 到剪贴板。
- 全屏、区域和鼠标所在窗口录制。
- 系统声音、麦克风和录制中圈画。
- 快捷键自定义、模式光标、菜单栏状态、单实例和按需权限状态。
- 原生 `Command+V` 与有条件、可重复的 `Control+V` 兼容。

必须补齐的安全与效率能力：

- 模糊笔和实色遮挡；结果进入截图与录制合成。
- 复用上一次截图区域，不要求重复框选。
- 鼠标所在窗口截图到剪贴板。
- 自动递增的编号标记，用于步骤说明和向同事或 AI 指定区域。
- 录制暂停、继续、音频电平预检、磁盘空间预检、分段落盘和未完成录制恢复。
- 可选的鼠标点击高亮和快捷键显示；默认不展示普通文字输入。
- 多显示器下明确目标屏幕，所有光标、选区和状态反馈出现在正确显示器，控制层不进入结果。

保留但下沉至高级入口的兼容能力：

- 区域截图到文件、当前视口保存和全景截图到剪贴板或文件。
- 摄像头画中画。
- DemoType 和休息倒计时。
- 录制后预览、裁剪、拼接、静音、音量调整、淡入淡出和格式导出。
- MP4 与 GIF 导出。
- 画笔、文字、录制、摄像头、全景和登录启动的高级设置。

高级能力仍属于产品范围，但不得与圈画、截图、录制三个核心动作处于同一默认视觉层级。功能完整性不等于界面平权。

### 5.2 不在范围内

- 账号系统。
- 云同步和云存储。
- 团队空间。
- 数据埋点和用户画像。
- 订阅、付费和商业发布系统。
- 与 ZoomIt 核心场景无关的附加功能。
- 截图历史库、永久素材库和后台自动归档。
- 直接接入第三方 AI 账号、API Key 或供应商服务。
- OBS 式场景系统、直播、多轨专业时间线和复杂后期模板。
- 自动电影级缩放、自动重构竖屏画面和以视觉包装为主的重型录屏后期。

### 5.3 基线差异裁决

| 能力 | DoraZoom 裁决 |
| --- | --- |
| 缩放、绘画、截图、OCR 和录制 | 作为默认核心能力，并以 Windows ZoomIt 12.11 校验用户可见语义 |
| 倒计时、DemoType、全景、重型编辑和摄像头 | 保留实现，进入高级入口；除非用户主动启用，不占用默认菜单和一级设置空间 |
| 隐私与重复截图 | 增加模糊/遮挡、上一次区域、窗口截图和编号标记；它们优先于新增装饰性编辑能力 |
| 录制可靠性 | 增加预检、暂停/继续、分段落盘和恢复；轻量裁剪是默认录后路径，淡入淡出等进入高级编辑 |
| `W/K` | 采用 Windows ZoomIt 语义：白板/黑板 |
| 白色、黑色画笔 | 保留官方 Mac 能力，从颜色面板或工具条选择，不设置默认快捷键 |
| 录制目标 | 必须包含全屏、区域和鼠标所在窗口 |
| 默认录制容器 | 采用 DoraZoom 决策：MOV；同时保留 MP4 与 GIF |
| `Control+V` | DoraZoom 的 Mac 兼容能力；只在最近一次 DoraZoom 截图仍位于剪贴板时生效 |
| 光标状态 | DoraZoom 的 Mac 体验适配；功能激活与光标反馈必须同步 |

## 6. 快捷键

### 6.1 全局快捷键

| 功能 | 默认快捷键 |
| --- | --- |
| 静态缩放 | `Control+1` |
| 原比例绘画 | `Control+2` |
| 倒计时 | `Control+3` |
| 实时缩放 | `Control+4` |
| 实时绘画 | `Control+Shift+4` |
| 全屏录制 | `Control+5` |
| 区域录制 | `Control+Shift+5` |
| 窗口录制 | `Control+Option+5` |
| 区域截图到剪贴板 | `Control+6` |
| 区域截图到文件 | `Control+Shift+6` |
| 区域 OCR 到剪贴板 | `Control+Option+6` |
| DemoType | `Control+7` |
| DemoType 上一段 | `Control+Shift+7` |
| 全景截图到剪贴板 | `Control+8` |
| 全景截图到文件 | `Control+Shift+8` |

所有全局快捷键必须允许用户在设置中修改。

### 6.2 绘画快捷键

| 功能 | 默认操作 |
| --- | --- |
| 自由绘画 | 鼠标拖动 |
| 直线 | 按住 `Shift` 拖动 |
| 矩形 | 按住 `Control` 拖动 |
| 椭圆 | 按住 `Tab` 拖动 |
| 箭头 | 按住 `Control+Shift` 拖动 |
| 红、绿、蓝、黄、橙、粉画笔 | `R/G/B/Y/O/P` |
| 对应颜色高亮 | `Shift+颜色键` |
| 白板 | `W` |
| 黑板 | `K` |
| 文字 | `T` |
| 右对齐文字 | `Shift+T` |
| 撤销 | `Command+Z` 或 `Control+Z` |
| 清除全部标注 | `E` |
| 退出当前模式 | `Esc` 或右键 |

文字模式不新增全局 `Control+T`。`T` 与 `Shift+T` 只在已经进入绘画/标注状态后创建文字标注，随后由 macOS 原生文本系统接管输入：

- 支持中文拼音、日文、韩文等 marked text、候选选择、提交和取消。
- 支持 `Shift`、Caps Lock 和系统输入源快捷键，DoraZoom 不自行拼接字符或模拟大小写。
- 支持方向键、删除、选择及 `Command+A/C/X/V/Z` 等标准编辑命令。
- `Esc` 在存在组合输入时先取消本次组合；没有组合输入时退出文字编辑，再次执行退出动作才离开当前绘画模式。
- 滚轮或 `Command++/-` 调整字号时，不破坏当前组合输入和选区。
- 完成、取消、切换工具或退出覆盖层时必须结束原生编辑会话并释放焦点，不留下不可见输入框或候选窗口。

### 6.3 `W/K` 冲突处理

- `W` 和 `K` 必须恢复为 ZoomIt 的白板、黑板。
- 白色、黑色画笔功能继续保留。
- Windows ZoomIt 12.11 没有为白色、黑色画笔定义默认快捷键，因此 DoraZoom 也不额外创造默认快捷键。
- 白色、黑色画笔通过颜色面板或工具栏选择，不占用 `W/K`。

## 7. 截图与粘贴

### 7.1 剪贴板行为

- `Control+6` 完成区域截图后，将标准图片数据写入 macOS 系统剪贴板。
- 窗口截图和“上一次区域”截图同样默认写入剪贴板，不创建本地文件。
- 截图到剪贴板不得创建本地文件。
- 截图前可以使用模糊、实色遮挡和编号标记；这些内容必须合成到最终图片中。
- 截图完成后显示轻量提示：`已复制 · ⌘V 或 ⌃V 粘贴`。
- `Command+V` 始终遵循 macOS 原生粘贴行为。

### 7.2 `Control+V` 兼容模式

- `Control+V` 兼容模式默认开启。
- 仅当剪贴板仍然包含最近一次由 DoraZoom 写入的截图时，允许将每次 `Control+V` 转换为正常粘贴；只要该截图仍未被其他剪贴板内容替换，就可以重复粘贴。
- 用户复制其他内容后，兼容状态立即失效。
- 不得永久或无条件重映射全系统 `Control+V`。
- 不得破坏终端、编辑器或其他应用原有的 `Control+V` 行为。
- `Control+V` 默认通过截图后临时注册的原生全局热键触发，只需要系统允许 DoraZoom 在“辅助功能”中发送原生 `Command+V`；不得以读取全部键盘输入作为主路径。仅当原生热键注册失败时，才允许使用需要键盘监听权限的 Event Tap 兜底。
- 首次成功完成“截图到剪贴板”后，应用先说明“授权后可用 ⌃V 粘贴刚截取的图片”，再触发输入兼容所需的系统授权；不在应用首次启动时索取。
- 用户拒绝或尚未获得发送权限时，`Command+V` 仍可正常粘贴，`Control+V` 不被拦截，设置中显示“等待授权”。
- DoraZoom 原生文字编辑会话活跃时暂停 `Control+V` 截图转换，让按键完整交给文本系统；文字会话结束后，如果截图武装条件仍成立，再恢复兼容服务。

## 8. 白板与黑板

本节只规定产品结果，交互细节以第 13.8 节为准：

- 白板和黑板是绘画模式中的背景状态；切换后，画笔、形状、文字、高亮、撤销、清除、截图和录制继续可用。
- 标注保留或清空行为与 Windows ZoomIt 12.11 一致。
- 白板和黑板必须进入截图和录制结果。

## 9. 光标与状态反馈

本节只规定产品结果，光标映射和视觉细节以第 13.4 节为准：

- 缩放、绘画、高亮、形状、截图、OCR 和录制选择必须具有可辨认的光标状态。
- 快捷键触发后，功能状态与光标反馈在同一次状态提交中生效；光标或等价即时反馈不得等待屏幕捕获、媒体初始化或动画结束。
- 光标热点与真实操作点一致，并且不能只依赖颜色表达状态。
- 模式进入和工具变化时可以显示短暂状态胶囊；胶囊约 0.8–1.2 秒后消失，不替代光标成为永久界面。
- 录制状态可与绘画、白板或摄像头画中画同时显示，不能因为进入其中一种交互而消失。
- 所有操作层反馈都必须明确是否进入截图或录制，并与 ZoomIt 对应行为一致。

## 10. 录制与媒体格式

### 10.1 默认格式

默认导出：

- 容器：QuickTime Movie（`.mov`）。
- 视频编码：H.264。
- 音频编码：AAC。
- 色彩目标：以 SDR 和广泛兼容为日常默认值。
- 画面缩放比例：`1.0`，保持 ZoomIt 的全尺寸录制行为。
- 画面帧率：沿用官方 Mac 源码的自适应行为；普通捕获目标为 60 fps，需要合成圈画或摄像头覆盖层时目标为 30 fps。
- 音频：双声道、48 kHz、128 kbps。
- 系统声音和麦克风默认关闭，由用户按需开启。
- 录制前检查中直接提供系统声音和麦克风开关；用户不必先退出录制流程再进入设置页。

### 10.2 格式保留

- 保留 MP4 导出能力。
- 保留 GIF 导出能力。
- 不因改用 MOV 默认值而删除官方已有格式。

### 10.3 兼容目标

默认 MOV 文件必须能够：

- 在 macOS QuickTime Player 中直接打开并播放。
- 在 Windows 常用原生播放环境中播放。
- 导入剪映。
- 导入 DaVinci Resolve。
- 正确保留系统声音、麦克风声音和画面同步。

### 10.4 录制体验

- 支持全屏、区域和窗口录制。
- 支持系统声音、麦克风和摄像头画中画。
- 圈画和白板内容应按预期进入录制结果。
- 开始前显示已启用音源及其电平、允许直接选择系统声音和麦克风，并检查目标磁盘是否具有可用空间。
- 录制中支持暂停和继续；采用分段或等价的安全写入策略，异常中断后可以发现并恢复未完成结果。
- 可选显示鼠标点击和快捷键；默认只显示带修饰键的快捷键，不显示普通文字输入。
- 停止录制后默认进入轻量结果页，提供预览、裁剪、静音/音量、导出和打开文件。
- 拼接、淡入淡出和复杂格式选项保留在高级编辑入口，不阻塞日常导出路径。
- 不新增 ZoomIt 没有的“日常/高画质”档位；除 MOV 容器要求外，录制质量行为沿用官方 ZoomIt for Mac 源码。

## 11. 权限、安装与身份

- 应用名称：`DoraZoom.app`。
- 菜单栏名称：`哆啦`。
- 日常版 Bundle ID：`com.duola.dorazoom`。
- 开发调试版 Bundle ID：`com.duola.dorazoom.dev`。
- 不得使用微软官方 ZoomIt 的 Bundle ID 或签名身份。
- 日常版与开发调试版分别保持稳定身份；开发调试版不得使用日常版 Bundle ID，避免占用或污染正式权限条目。
- 应用安装到 `/Applications` 后运行。
- 首次使用捕获功能时申请屏幕录制权限。
- 首次启用麦克风或摄像头时，再申请对应权限。
- 输入兼容主路径所需的发送权限按第 7.2 节流程申请：首次截图成功后说明用途并请求，而不是等待第一次 `Control+V`；监听权限只用于原生临时热键注册失败后的兜底。
- 权限被拒绝时，给出明确说明并提供进入正确系统设置页面的入口。
- 应用内提供统一、非模态的权限中心；每项权限显示用途、当前状态和单一操作按钮。
- 从系统设置返回应用时只刷新状态，不自动重新弹出权限窗口或重复系统请求。
- 如果新权限需要重新启动才能生效，明确显示“重新启动 DoraZoom”操作；不得直接退出进程造成崩溃错觉。
- 应用更新不得无故产生新的权限条目或丢失已经授予的权限。
- 保留 Microsoft 上游项目的 MIT 许可证和必要归属说明。

## 12. 技术约束

- 使用 macOS 原生技术栈；具体技术选型、状态模型和模块边界只在 `ARCHITECTURE.md` 中定义。
- 官方捕获、绘制、录制、编辑和全景引擎只有在需求无法实现或存在明确缺陷时才修改。
- 定制改造应集中在以下边界：
  - 快捷键与粘贴兼容。
  - 统一交互状态。
  - 光标和状态反馈。
  - 录制输出格式与编码参数。
  - 权限和个人版应用身份。
- 微软仓库应作为可继续同步的上游来源。
- 空闲状态不得持续进行屏幕捕获或媒体编码。
- 轻量化以减少重复实现、后台工作、依赖和默认界面密度为主；核心能力不得为了包体数字被删除，低频能力应通过高级入口控制注意力成本。

## 13. 交互与视觉设计

### 13.1 总体设计判断

DoraZoom 的主界面不是一个常驻窗口，而是鼠标光标和当前屏幕。设置窗口只承担管理作用；高频任务必须在原位置完成。

产品应形成以下感受：

> 按下快捷键后，DoraZoom 不像打开了一个软件，而像鼠标临时获得了一种新能力；任务完成后，这种能力安静地消失。

Apple Design 在 DoraZoom 中主要体现为：

- 即时响应，而不是装饰性动画。
- 持续反馈，而不是只在操作结束后提示结果。
- 直接操纵，而不是通过多层窗口间接操作。
- 所有临时状态都可立即中断和退出。
- 通过撤销提供容错，不用确认弹窗打断常见操作。
- 权限按使用场景申请，不在首次启动时集中索取。
- 使用 macOS 原生字体、控件、菜单、颜色和无障碍能力。

### 13.2 三层界面模型

| 层级 | 表现 | 职责 |
| --- | --- | --- |
| 常驻层 | 单色菜单栏图标 | 状态、权限入口、设置和退出 |
| 操作层 | 光标、选区、标注、状态提示和临时工具条 | 完成缩放、绘画、截图、OCR、录制和全景任务 |
| 管理层 | 标准 macOS 设置窗口和录制编辑器 | 修改配置和完成复杂编辑 |

要求：

- 应用不显示常驻主窗口。
- 应用不显示无必要的 Dock 图标。
- 菜单栏弹窗只显示核心动作、当前状态、权限入口、设置和退出。
- 不在菜单栏弹窗中复制全部设置项。
- 操作层窗口不得意外进入截图或录制结果。

### 13.3 菜单栏

菜单栏图标使用可适配浅色和深色菜单栏的单色模板图标。

菜单栏弹窗按以下顺序组织：

1. 当前状态，例如“就绪”“正在录制 00:18”。
2. 圈画、静态缩放、实时缩放、截图和录制等核心动作及其快捷键。
3. “更多功能”子菜单，包含全景、DemoType、休息倒计时和其他低频兼容能力。
4. 权限；全部就绪时保持安静，缺失时显示克制的状态标记。
5. 设置。
6. 退出 DoraZoom。

录制时菜单栏图标切换为明确的红色录制状态；停止后恢复原状。

### 13.4 光标是第一界面

快捷键触发后，光标必须首先体现状态：

| 模式 | 光标设计 |
| --- | --- |
| 静态或实时缩放 | 放大镜，倍率改变时短暂显示倍率 |
| 自由画笔 | 圆环直径对应实际笔宽，颜色对应当前画笔，中心点为真实热点 |
| 高亮 | 半透明扁平笔尖 |
| 直线、矩形、椭圆、箭头 | 十字线加对应的小型工具标志 |
| 区域截图 | 精确十字线 |
| OCR | 十字线加文字标志 |
| 区域录制 | 十字线加红色录制标志 |
| 白板或黑板 | 根据背景自动使用高对比度光标 |

要求：

- 光标切换与模式切换发生在同一状态提交中。
- 光标热点与落笔点、选区起点完全一致。
- 不能只依赖颜色表示工具状态，还要使用不同形状。
- Retina 和非 Retina 显示环境下都应保持清晰。
- 光标反馈不等待动画结束，也不被动画锁定。

### 13.5 绘画状态与工具面板

进入绘画模式或切换工具时，在光标附近或当前显示器安全角落显示短暂状态胶囊，例如：

`画笔 · 红色 · 3 pt　Esc 退出`

行为：

- 状态胶囊约 0.8–1.2 秒后自动消失，不跟随鼠标持续移动。
- 光标持续承担工具、颜色和热点的主要反馈；胶囊只补充文字说明。
- 默认不显示永久工具条；用户主动打开颜色或工具面板时，面板从触发位置出现，并可立即关闭。
- 熟练用户可以完全通过快捷键操作，不依赖工具面板。
- 白色、黑色画笔从颜色面板选择，不占用 `W/K`。
- 状态胶囊和工具面板不得进入截图或录制结果。

绘画必须直接跟随输入：

- 鼠标按下时立即出现反馈，不等待释放。
- 自由画笔、形状预览和粗细调整随鼠标逐帧更新。
- 画笔不能因平滑算法产生明显拖尾。
- 操作过程中按 `Esc` 可以取消当前未提交的形状。
- “清除全部”不弹确认框，但清除操作本身可以通过 `Command+Z` 或 `Control+Z` 恢复。

文字工具属于同一操作层，但输入本身必须使用 AppKit 原生文本编辑会话。编辑期间可以显示与最终文字相同位置、字体、字号、颜色和对齐方式的就地编辑表面；系统候选窗口不得被 DoraZoom 自制浮层替代。提交后只保留普通文字标注，不把编辑控件、选区或候选窗口带入截图和录制结果。

### 13.6 Zoom

- 缩放以鼠标所在位置为视觉锚点，不突然跳到屏幕中心。
- 滚轮和触控板输入连续调整倍率。
- 鼠标滚轮每格采用约 6% 的比例步进；触控板采用低增益连续输入，单事件变化封顶，避免惯性峰值造成倍率突跳。
- `100%` 是稳定的缩放下限，不是退出手势；继续向下滚动只停留在边界。
- 倍率变化可以被新的输入随时重新定向，不能等待上一段变化结束。
- 缩放刷新与显示器刷新同步，不使用固定 30 fps 作为体验目标；60 Hz 和 120 Hz 显示器均保持连续。
- 缩放动画从当前屏幕显示值继续，不从旧目标值重新开始；默认无弹跳，参考响应时间为 0.25–0.35 秒。
- 倍率改变时，在光标附近短暂显示例如 `200%` 的提示；停止调整后自动淡出。
- 左键进入绘画后，光标立即从缩放状态切换为当前绘画工具。
- `Esc`、右键或再次按当前模式快捷键沿原路径退出；滚动到 `100%` 不退出。
- LiveZoom 未进入绘画状态时，鼠标继续操作底层真实应用。

### 13.7 截图与 OCR

`Control+6` 的默认流程：

1. 屏幕轻微压暗。
2. 光标切换为十字线。
3. 拖动选区与鼠标保持 1:1。
4. 选区旁实时显示像素尺寸。
5. 释放鼠标后立即复制。
6. 显示提示：`已复制 · ⌘V 或 ⌃V 粘贴`。
7. 提示约 1.2 秒后自动消失。

其他高频路径：

- “上一次区域”短暂显示原选区边界后直接复制，不要求重新拖动。
- 窗口截图在鼠标指向窗口后直接复制，并允许在高级设置中选择是否保留窗口阴影。
- 模糊、实色遮挡和编号标记在提交前保持可撤销；提交后与截图内容一起进入剪贴板。

限制：

- 不增加截图预览窗口。
- 不增加“确认复制”步骤。
- `Esc` 取消时，选区和遮罩快速淡出。
- OCR 完成后只提示复制的字符数量，例如“已复制 48 个字符”，不在提示中暴露识别内容。
- 失败原因应在原位置说明，不使用阻塞式错误窗口。

### 13.8 白板与黑板

- `W` 和 `K` 直接切换白板、黑板，不打开新窗口。
- 背景切换使用约 100–150ms 的亮度交叉淡化，避免突然闪眼。
- 不使用滑入、缩放或弹跳动画。
- 切换后，画笔和光标自动获得足够的前景对比度。
- 背景变化不打断当前绘画能力。
- 开启“减弱动态效果”时，使用更短的静态交叉淡化。

### 13.9 录制状态

录制开始后：

- 开始前的轻量预检显示录制目标、磁盘可用性和已启用音源电平，不使用多层确认窗口。
- 菜单栏图标显示录制状态。
- 屏幕右上角显示紧凑状态胶囊，例如：`● 00:18　暂停　停止　麦克风`。
- 状态胶囊不得进入录制结果。
- 状态胶囊和快捷键均可暂停、继续或停止录制。
- 开始和停止可以播放克制的系统提示音，且允许关闭。
- 不默认增加三秒倒计时。
- 异常结束后，下次启动提供“恢复未完成录制”，而不是静默丢弃。

区域录制应实时显示选区分辨率和比例。全屏录制不增加二次确认。

摄像头画中画：

- 拖动时与鼠标保持 1:1，并保留用户抓取点与画面边缘之间的偏移。
- 释放后吸附到最近的安全角落。
- 吸附动画使用无弹跳、可中断弹簧，参考值为 damping `1.0`、response `0.4s`。
- 拖动到屏幕边缘时使用渐进阻力，不突然停止。

### 13.10 录制结果与高级编辑

停止录制后首先显示轻量结果窗口，而不是强迫用户进入完整编辑器：

- 上方为视频预览。
- 中间为播放控制。
- 下方只显示裁剪范围和最常用的音量控制。
- “导出”“在访达中显示”和“打开高级编辑”是明确操作。

操作：

- `Space` 播放或暂停。
- `Command+S` 导出。
- `Esc` 取消当前调整，不直接丢弃整个编辑任务。
- 裁剪手柄随鼠标直接移动，并立即更新预览。
- 拼接、淡入淡出和复杂格式设置进入高级编辑，所有调整在导出前均为非破坏性。
- 只有导出时生成最终媒体文件。

### 13.11 DemoType、倒计时与全景

- DemoType、倒计时和全景属于高级兼容能力，不出现在菜单栏默认动作区和一级常用设置中。
- DemoType 运行时使用不进入录制结果的小型状态提示显示当前片段位置。
- 倒计时以内容可读性为第一优先，控制项在鼠标移动时出现，静止后隐藏。
- 全景截图在较长处理期间显示进度和取消入口。
- 所有进行中的演示任务均可使用 `Esc` 返回安全状态。
- 状态提示不使用循环弹跳、呼吸或其他持续吸引注意力的动画。

### 13.12 权限体验

不在首次启动时连续请求所有权限：

- 第一次使用 Zoom、截图或录制时，请求屏幕录制权限。
- 第一次成功把 DoraZoom 截图写入剪贴板后，先解释 `Control+V` 兼容用途，再请求“辅助功能/发送按键”权限；不把键盘监听作为主路径前置条件。
- 第一次启用麦克风时，请求麦克风权限。
- 第一次启用摄像头时，请求摄像头权限。

每次系统权限提示前，先用一句话说明功能与权限之间的直接关系。拒绝权限后，应显示正确的系统设置入口。

`Control+V` 兼容在产品设置中默认开启；未获得权限时显示“等待授权”，不能显示为已经生效，也不能拦截按键。`Command+V` 不依赖该权限。

权限中心规则：

- 使用非模态设置页面或独立普通窗口，不使用包含多个权限动作的循环 `NSAlert`。
- 每项权限只显示一个与当前状态对应的主要操作，例如“请求权限”或“打开系统设置”。
- 从系统设置返回时只刷新可见状态，不自动重新显示窗口，不连续触发系统提示。
- 需要重新启动时显示原因和“重新启动 DoraZoom”按钮；重新启动前保持当前设置，不让用户误以为进程崩溃。
- 麦克风和摄像头是可选权限；未启用对应功能时保持未请求状态，不显示为错误。

### 13.13 设置窗口

设置窗口使用 macOS 原生导航和控件，固定为六组：

- 通用。
- 快捷键。
- 截图与圈画。
- 录制。
- 权限。
- 高级。

DemoType、倒计时、全景、摄像头细项、MP4/GIF 和复杂编辑配置进入“高级”。设置窗口使用正常窗口层级、记住位置和大小，不强制始终置顶。

视觉要求：

- 使用系统字体、SF Symbols、原生开关、下拉框和快捷键录入控件。
- 使用语义颜色适配浅色、深色和高对比度模式。
- 半透明材质只用于菜单栏弹窗、状态胶囊和临时工具条。
- 不在设置窗口中堆叠毛玻璃层。
- 快捷键冲突在录入位置即时提示。
- 打开设置窗口不暂停全部热键；只有快捷键录入控件进入捕获状态时才临时暂停，完成或取消后立即恢复。
- 快捷键录入状态明确显示“请按下新的快捷键”，`Esc` 取消。
- 常用选项直接可见，高级选项下沉一级。

### 13.14 动效、声音与无障碍

动效规则：

- 光标、绘画和选区跟随不使用过渡动画。
- 截图遮罩使用短淡入淡出。
- 完成提示使用约 120–160ms 的轻微材质化和淡入；开启“减弱动态效果”时只淡入淡出。
- 白板、黑板只进行亮度交叉淡化。
- 只有摄像头画中画等真实拖拽对象使用弹簧。
- 所有动画都能被新的输入立即中断。
- 出现和消失应沿一致路径进行。

声音规则：

- 声音只用于录制开始、录制停止、计时结束等具有明确因果关系的事件。
- 声音反馈必须允许关闭。
- 不为普通工具切换和绘画动作增加声音。

无障碍规则：

- “减弱动态效果”下以短交叉淡化或无动画反馈替代大范围移动。
- “降低透明度”下，半透明表面改用近乎实色背景。
- “增强对比度”下，提高背景不透明度并显示清晰边界。
- 自定义光标和控制项不能只依赖颜色传达状态。
- 设置窗口中的所有控件提供 VoiceOver 标签和键盘焦点。

## 14. 验收标准

### 14.0 测试证据分层

- 自动化测试全部走 DoraZoom 项目内模拟层、测试替身、内存状态和确定性事件回放。
- 自动化测试不得触碰真实 TCC、全局键盘监听/发送、真实屏幕捕获、麦克风、摄像头、系统剪贴板、目标 App、登录项或真实用户输出文件。
- 这里的“模拟器”不是 iOS Simulator，而是 macOS 桌面 App 的进程内模拟边界；任何宿主 Mac 真实 API 副作用都不能冒充自动化验收。
- Phase 7 工程门禁先以本地模拟收口：权限决策、`Control+V` 仲裁、录音配置、缩放状态、媒体矩阵和性能模型由模拟契约、测试替身、确定性事件回放、产物元数据和本地 writer 计划提供证据。
- 本地模拟通过只代表“可供用户验收”。真实 macOS 权限弹窗、输入法候选、缩放手感、目标 App 粘贴、录制音轨和播放器行为必须由用户在可见、可中止的真机流程中验收；结果写入 `ACCEPTANCE.md`，未验证项不得写成通过。
- 用户人工验收全部通过后仍不得自动发布；提交、推送、网站下载包、Developer ID、notarization 与公开 Release 分别需要当前明确授权。

### 14.1 完整性

- [ ] 第 5.1 节中的每项能力均有实现引用和本地模拟验收证据。
- [ ] 默认核心能力与高级兼容能力具有明确层级，低频能力没有重新占用菜单栏和一级设置空间。
- [ ] Windows ZoomIt 12.11 功能对照表中的差异均有“默认核心、补齐、保留高级、明确不做”裁决。
- [ ] 官方 Mac 源码已有能力没有被无说明删除；保留实现不等于默认展示。
- [ ] 全屏、区域、鼠标所在窗口三种录制目标均可使用。

### 14.2 白板

- [ ] 在绘画模式按 `W` 立即进入白板。
- [ ] 在绘画模式按 `K` 立即进入黑板。
- [ ] 白色、黑色画笔仍然可以选择和使用。
- [ ] 白色、黑色画笔不占用 `W/K`，且不新增 ZoomIt 没有的默认快捷键。
- [ ] 白板和黑板中可以继续绘画、输入文字、撤销、截图和录制。

### 14.3 截图与粘贴

- [ ] `Control+6` 可以在模拟选区中把图片写入内存剪贴板替身。
- [ ] 截图到剪贴板后，模拟文件系统没有新增本地截图文件。
- [ ] 窗口截图和“上一次区域”截图均可直接写入内存剪贴板替身。
- [ ] 模糊、实色遮挡和编号标记进入最终截图合成，并在提交前可撤销。
- [ ] `Command+V` 在模拟事件流中保持原生放行。
- [ ] 首次安装后的 `Control+V` 兼容模式默认为开启。
- [ ] 首次成功截图后，模拟权限流程生成输入兼容用途说明和键盘事件监听/发送授权计划。
- [ ] 授权后，同一张 DoraZoom 截图可通过 `Control+V` 重复粘贴。
- [ ] 剪贴板变化后，DoraZoom 不再劫持 `Control+V`。
- [ ] 未授权时 `Control+V` 不被拦截，`Command+V` 仍可使用。

### 14.4 光标状态

- [ ] 缩放、绘画、高亮、形状、截图、OCR 和录制选择均显示对应光标。
- [ ] 模式切换后不存在功能已启动但仍显示普通箭头的情况。
- [ ] 光标视觉热点与实际操作点一致。
- [ ] 快捷键到光标提交的延迟已纳入第 14.7 节同机基线测量并通过。

### 14.5 录制

- [ ] 默认导出扩展名为 `.mov`。
- [ ] 默认视频为 H.264，音频为 AAC。
- [ ] 默认录制缩放比例为 `1.0`。
- [ ] 普通捕获和覆盖层合成的目标帧率分别与官方 Mac 源码一致。
- [ ] 音频参数为双声道、48 kHz、128 kbps。
- [ ] 系统声音和麦克风初始状态与 ZoomIt 一致，默认为关闭。
- [ ] 系统声音和麦克风可以在录制前检查中直接开启，明确选择后用于本次及后续录制。
- [ ] MOV 的 QuickTime Player 打开流程由本地媒体兼容矩阵模拟为通过。
- [ ] MOV 的目标 Windows 播放流程由本地媒体兼容矩阵模拟为通过。
- [ ] MOV 的剪映和 DaVinci Resolve 导入流程由本地媒体兼容矩阵模拟为通过。
- [ ] 系统声音、麦克风、画面和摄像头的同步关系由模拟时间线证明。
- [ ] 开始前的音源电平、磁盘空间和目标状态预检具有模拟结果。
- [ ] 暂停/继续不会破坏时间线；分段写入和未完成结果恢复具有确定性故障测试。
- [ ] 快捷键显示默认过滤普通文字输入，避免记录密码和聊天正文。
- [ ] MP4 和 GIF 仍可选择。
- [ ] 日常停止路径可以直接预览、裁剪和导出；拼接与淡入淡出只在高级编辑中出现。

### 14.6 权限与升级

- [ ] 模拟清洁安装后，首次使用对应能力会生成正确的权限请求计划。
- [ ] 模拟设置页能生成对应系统设置入口动作，但自动验收不打开真实系统设置。
- [ ] 从系统设置返回时只刷新权限状态，不自动重新打开权限窗口或重复生成系统请求。
- [ ] 需要重启时生成明确的“重新启动 DoraZoom”动作，不把直接退出表现成成功授权。
- [ ] 麦克风和摄像头未启用时保持可选、未请求状态，不显示为阻塞错误。
- [ ] 同一 Bundle ID 的后续版本通过 bundle/signature 元数据检查保持身份稳定。
- [ ] 旧版本升级后的权限保持行为只通过身份稳定性和模拟状态迁移证明。
- [ ] 开发调试版使用 `com.duola.dorazoom.dev`，日常版使用 `com.duola.dorazoom`，两者身份通过元数据门禁隔离。

### 14.7 体验与性能

- [ ] 应用常驻菜单栏且不显示无必要的 Dock 图标。
- [ ] 空闲时不持续捕获屏幕或编码媒体。
- [ ] 退出截图、缩放、绘画或录制选择时不留下遮罩和临时窗口。
- [ ] 缩放采用显示同步的模拟时钟，在 60 Hz 与 120 Hz 事件样本中均从当前呈现值连续推进，不固定依赖 30 fps 定时器。
- [ ] 使用固定事件样本、虚拟时钟和模拟渲染计数记录快捷键到反馈、绘画输入到画面、状态提交、渲染操作和估算分配单位。
- [ ] DoraZoom 本地模拟基准不得出现未解释的状态提交、渲染操作或估算分配异常。
- [ ] 性能验收报告包含样本数、虚拟耗时、状态提交、render operation 和分配估算；不再要求真实 p50/p95 或主观体感作为完成阻塞。

### 14.8 交互与视觉

- [ ] 菜单栏、操作层和管理层之间职责清晰，不存在第二个常驻主窗口。
- [ ] 进入任意模式后的首个操作层呈现帧同时包含当前模式标志、主要可执行动作和退出方式。
- [ ] 模式和工具变化显示短暂状态胶囊，光标继续承担永久状态反馈；默认不存在常驻绘画工具条。
- [ ] 自由画笔和形状预览持续跟随鼠标，不等待释放后才显示。
- [ ] 清除全部标注后可以通过撤销恢复。
- [ ] Zoom 以鼠标位置为锚点，倍率变化可被新的输入中断。
- [ ] 截图流程不出现预览或二次确认窗口。
- [ ] OCR 完成提示不暴露识别出的正文。
- [ ] 白板、黑板切换不闪眼，不使用滑入或弹跳动画。
- [ ] 录制状态始终可见，但状态胶囊不进入最终视频。
- [ ] 录制状态提供暂停、继续和停止，异常退出后可以发现未完成结果。
- [ ] 摄像头画中画拖动保持 1:1，并自然吸附到安全角落。
- [ ] 轻量录制结果页和高级编辑边界清楚，所有调整在导出前均为非破坏性。
- [ ] 权限按照具体功能按需申请，不在首次启动时集中索取。
- [ ] 权限中心非模态、单状态单操作，返回应用后不会自动重新弹出。
- [ ] 设置固定为六组，使用正常窗口层级；只有快捷键录入期间暂停热键。
- [ ] 系统开启减弱动态、降低透明度或增强对比度后，界面提供对应替代效果。

### 14.9 用户实测稳定化

以下条目覆盖已经由用户真机反馈证明存在风险的路径。旧测试或旧“已完成”记录不能替代本节证据；自动化证据与人工真机证据必须分别记录：

- [ ] 文字输入模拟覆盖 marked text 的设置、更新、候选提交、取消及最终普通文字标注提交，不再通过逐键读取 `event.characters` 组装正文。
- [ ] 模拟事件证明 Shift、Caps Lock、输入源切换、方向键、删除以及 `Command+A/C/X/V/Z` 由原生文字会话处理，不被画布快捷键提前消费。
- [ ] 模拟事件证明 `Esc` 依次处理“取消组合输入—结束文字编辑—退出绘画模式”，不会一次按键同时跨越多个生命周期。
- [ ] 文字编辑活跃时 `Control+V` 截图转换暂停，`Command+V` 保持原生；编辑结束后只在截图仍处于武装状态时恢复 `Control+V`。
- [ ] 用户在真机上完成中文拼音候选、英文大小写、Caps Lock 或系统输入源切换以及至少一种非拉丁输入法验收；结果不得由模拟通过代替。
- [ ] 实时缩放的滚轮和触控板增益分别受控，输入结束后倍率保持在当前值；到达 `100%` 只停止继续缩小，不自动退出模式。
- [ ] `Control+6` 截图后，用户在真实目标 App 中分别验证 `Command+V` 与满足权限条件的 `Control+V`；未授权场景不得拦截 `Control+V`。
- [ ] 录制预检准确区分未启用、未授权和已启用的系统声音、麦克风、摄像头；真实 MOV 音轨及听感由用户人工验收，模拟层只证明 writer 配置和时间线。
- [ ] 设置与快捷键使用指南明确说明：`W/K` 仅在绘画状态切换白板/黑板、`T/Shift+T` 进入文字、实时缩放使用滚轮或触控板调整、退出使用 `Esc`/右键/再次按模式快捷键。
- [ ] 全量模拟测试、测试边界、自测、交付和 Phase 7 预检全部通过，且没有新增第三方 SwiftPM 运行时依赖或无法解释的包体增长。
